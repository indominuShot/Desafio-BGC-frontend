import { Container, Form, FormHeader, FormMain, FormFooter } from './styles';

export default function FormLogin() {
  return (
    <Container>
      <Form>
        <FormHeader></FormHeader>
        <FormMain></FormMain>
        <FormFooter></FormFooter>
      </Form>
    </Container>
  );
}
